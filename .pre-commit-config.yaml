# Quarto extensions under _extensions/ are vendored third-party code. The
# hygiene hooks would rewrite them, and the next `quarto add` would undo it.
exclude: ^_extensions/

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: check-added-large-files
        args: ["--maxkb=1024"]
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: check-json
      - id: end-of-file-fixer
      - id: mixed-line-ending
      - id: trailing-whitespace

  - repo: https://github.com/domluna/JuliaFormatter.jl
    rev: v2.5.5
    hooks:
      - id: julia-formatter
